# No Cypher Fences

This markdown file has no Cypher code blocks at all.

It contains only prose and a non-Cypher code block:

```json
{"key": "value"}
```

There are no `cypher` fences here, so no diagnostics should be emitted.
